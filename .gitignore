.DS_Store
Thumbs.db
*.log
.claude/

# Cloudflare deploy credentials. This repo is PUBLIC, so a committed token
# would be world-readable the moment it is pushed.
.cf-netgenx.env
.env
*.env

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
